#!/bin/bash

set -euo pipefail

fixture=/tmp/omarchy-onscreen-keyboard-detector-devices.json
if [[ ${1:-} == -j && ${2:-} == devices && -s $fixture ]]; then
  exec /usr/bin/cat "$fixture"
fi

exec /usr/bin/hyprctl "$@"
